// Build script placeholder - server is run via tsx in development
// For production, we use tsx to run the server directly
// In a more advanced setup, we'd bundle the server with esbuild
console.log("Server build: use 'tsx src/server/index.ts' to run")
